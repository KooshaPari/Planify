name: self-merge-gate

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on: [pull_request_review]

permissions:
  contents: read

jobs:
  gate:
    uses: ./.github/workflows/pheno-self-merge-gate.yml
