name: Alert Sync Issues

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manifest-gate:
    uses: KooshaPari/phenotype-ops/.github/workflows/manifest-gate.yml@main
    with:
      fallback: warn
